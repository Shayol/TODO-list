<%= form_for([@task, @project]) do |f| %>
  <div class="col-md-4 col-md-offset-2">
    <%= f.text_field :content, class: "form-control" %>
  </div>
  <div class="col-md-1">
    <%= f.submit "Add task", class: "btn btn-success" %>
  </div>
<% end %>